# Compatibility scripts

`repository_ai_pipeline.py` is only a compatibility launcher for older commands.
The real implementation is the Python package under `/python`.

Preferred command:

```bash
PYTHONPATH=python python3 -m repository_analysis
```
